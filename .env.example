CVT_URL=https://localhost:9443
CVT_USERNAME=nscale
CVT_PASSWORD=
CVT_INSECURE=true

# Teleport tunnel shown before circuits/stats when CVT_URL is localhost.
CVT_TSH_TUNNEL=tsh ssh -L 9443:192.168.10.3:9443 first_name.last_name@mobilekit-p-phy-device100
CVT_UI_URL=https://localhost:9443/cables_validation/

# Path to an xlsx you downloaded from SharePoint (not OneDrive sync).
SHAREPOINT_LOCAL_PATH=
